apps:
  - script   : ./server.js
    name     : 'node_example-app'
    instances: 1
    exec_mode: cluster
  - script: ./index.js
    name: 'worker'
    watch: true
    env:
      NODE_ENV: development
    env_production:
      NODE_ENV: production
    env_staging:
      NODE_ENV: staging